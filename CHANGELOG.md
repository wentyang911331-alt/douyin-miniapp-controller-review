# 变更记录

## v1.1.4 - 2026-05-29

### 目标

- 基于 `v1.1.2` 开发纯 UI 视觉换肤版本，并还原 UI 交付包视觉
- `v1.1.3` 保留为闲置历史阶段，不进入本次实现范围

### 范围

- 只调整视觉层：颜色、字体、间距、圆角、边框、阴影、图标风格、线条粗细和背景层级
- 不改功能、不改数据结构、不改交互逻辑、不改业务结论
- 不新增后端接口、不新增重型第三方依赖

### 当前落地

- 已接入 UI 交付包的深色 / 明亮模式 tokens
- 已更新控制器代表色和排行榜条形颜色
- 已按 UI 交付包调整软件 / 手感模块的 good / warn / bad 颜色规则
- 已替换圆角线性 SVG 图标资源，并保留小程序 `<image>` 可用的固定色资源策略
- 已补充明暗 / 选中 / 排序文字色 SVG 变体，页面按状态切换图标路径，不再依赖 CSS `filter` 给图标变色
- 已调整顶部、导航、chip、表格、排行榜、弹窗、水印、卡片阴影和边框层级
- 已适配抖音原生顶栏暗色模式，通过导航栏深色背景和白色前景覆盖 `反馈`、`三点`、`关闭` 等平台按钮
- 已修复高亮产品后横向滚动表格时，右侧数据文字覆盖左侧固定型号列的问题
- 已将说明弹窗改为主体可滚动结构，并压缩说明文案为更短的小白话，保证底部按钮在完整手机高度内可见
- 已补齐 UI 交付稿同名状态截图对照，以及软件最右侧横滑、完整弹窗、显示列面板三类风险定向截图
- 已优化自动验收脚本，截图完成后清理浏览器临时配置，仅保留轻量 HTML / PNG 验收产物
- 已保留 `制动巡航` 文案，不采纳 UI 包中的 `自动巡航`

### 已确认口径

- 控制器代表色和排行榜颜色以 UI 交付包为准
- 业务判断处颜色规则以 UI 交付包为准
- 业务文案以当前项目为准，继续使用 `制动巡航`，不改为 UI 包中的 `自动巡航`

## v1.1.2 - 2026-05-26

### 调整

- 页面标题更新为「2026年春季260级控制器对比手册」，并同步小程序系统导航栏标题
- 模块导航由数字/圆圈改为 `1. 硬件`、`2. 软件` 等简称
- UI 配色按已验收 Figma 稿调整为图标蓝高亮和低饱和灰蓝体系
- 主题色底选中标签、按钮和表格/排行榜切换项统一白字
- 二级标题右侧图标去底色并弱化显示
- SVG 图标线宽调整为 `1.45`

### 修复

- 修复明亮模式图标颜色和系统导航栏白底黑字同步
- 修复显示列浮层文字垂直对齐
- 修复 `驻车刹车灯` 等长文案溢出风险
- 修复排行榜 `升序/降序` 被挤压成竖排的风险

### 新增

- 恢复页面级水印「小拓子」，横向铺满、斜向密集、低透明度
- 将模块说明改为页面居中弹窗，并使用遮罩压暗背景
- 重写模块说明文案，改为面向小白用户的解释口径

### 清理

- 当前原生小程序运行入口去除「改装」字样，改为参考基线、参测控制器、对比控制器等表达
- 继续保留 `controller-review(1).html` 作为历史基线，不作为运行入口

## v1.1.1 - 2026-05-25

### 新增

- 新增原生抖音小程序项目结构
- 新增 `app.js`、`app.json`、`app.ttss`、`project.config.json`
- 新增 `pages/controller-review/index.ttml`
- 新增 `pages/controller-review/index.ttss`
- 新增 `pages/controller-review/index.js`
- 新增 `pages/controller-review/index.json`
- 新增本地图标资源 `assets/icons/*.svg`

### 迁移

- 将 1.1.0 静态 HTML 页面迁移为原生小程序页面
- 将静态测试数据迁移到小程序前端代码
- 将主题、高亮、徽章、排名、当前分区、分区视图、排序和隐藏列状态迁移到 `tt.getStorageSync` / `tt.setStorageSync`
- 将表格阅读体验迁移为 `scroll-view` 横向滚动结构
- 将说明浮窗、排行榜、排序、列显示控制等交互迁移为 `Page`、`data`、`setData`、`bindtap`

### 保留

- 保留 `controller-review(1).html` 作为 1.1.0 历史基线和对照页面
- 保留标题、水印、圆角图标体系、测评报告风格和核心数据展示能力
- 继续不恢复聚焦、录制、截图功能

### 验证

- 通过小程序页面 JS 语法检查
- 通过 JSON 配置解析检查
- 通过 `Page` / `tt` 模拟运行冒烟检查
- 确认 6 个模块、7 个产品行、排行榜切换、排序、隐藏列、高亮和缓存逻辑可运行
- 禁止项扫描未发现 `document`、`innerHTML`、浏览器 `localStorage`、`web-view`、HTML `table/tr/td/th/select` 等迁移禁用写法

## v1.1.0 - 2026-05-25

### 新增

- 页面标题更新为「2026 年春季控制线横屏对比手册」
- 增加页面级弱水印「小拓子横评」
- 为 6 个测试模块增加说明入口和说明小浮窗
- 增加圆角图标体系文档和图标预览页
- 页面图标统一为圆角线性 SVG 风格

### 调整

- 「最佳」标记采用星标 + 最佳的圆角徽章样式
- 排名角标保留 `#1/#2/#3`，并统一圆角徽章视觉
- 文档更新为 UI 已验收、前端实现已完成的状态

### 移除

- 移除聚焦模式入口和非高亮产品变暗逻辑
- 移除录制模式入口、状态和样式
- 移除截图按钮和截图提示入口

### 验证

- 通过脚本语法检查
- 通过本机浏览器渲染验证
- 验证标题、水印、说明浮窗、列显示、排行榜、排序和移动端显示
- 确认页面无相关控制台错误

## v1.0.0 - 2026-05-25

### 当前状态

- 建立项目文档体系
- 明确当前阶段保留静态 HTML 展示形态
- 明确后续按抖音小程序准备口径推进
- 明确当前不需要后端
- 明确采用 UI 子代理和前端子代理协作
- 明确标题、水印、说明小浮窗、图标统一和功能移除需求

### 版本边界

- 原始 HTML 文件尚未进入实现修改阶段
- 当前版本作为后续开发前的基线备份
