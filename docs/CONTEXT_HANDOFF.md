# 上下文交接文档

## 当前状态

项目中的核心原始页面文件为：

- `controller-review(1).html`

当前阶段已完成文档落地，尚未修改原始 HTML。

当前版本为 1.0.0，对应 Git 标签为 `v1.0.0`。该版本是正式页面开发前的文档和原始页面基线。

## 项目一句话

这是一个控制线横向测试结果的静态展示页，后续要调整为更像测评报告的移动端页面，并为未来迁移到抖音小程序做结构准备。

## 已确认口径

- 标题：2026 年春季控制线横屏对比手册
- 水印：小拓子横评
- UI 风格：测评报告
- 说明方式：模块标题旁信息图标，点击后弹出小浮窗
- 落地方式：暂时保留 HTML，不正式创建抖音小程序工程
- 小程序准备：后续代码尽量按小程序数据驱动、样式可拆分、交互可迁移的方向整理
- 后端：当前不需要
- 子代理：先启用 UI 子代理和前端子代理两个角色

## 当前非目标

- 不修改 `controller-review(1).html`
- 不创建后端
- 不创建完整小程序工程
- 不接入接口
- 不引入第三方依赖
- 不改测试数据

## 下一步

1. 基于文档启动 UI 子代理和前端子代理。
2. UI 子代理输出视觉与交互体验建议。
3. 前端子代理输出实现方案与风险清单。
4. 主控合并方案并交给用户确认。
5. 用户确认后再进入 HTML 修改。

## 重要文件

- 当前版本号：`VERSION`
- 版本记录：`CHANGELOG.md`
- 项目规范入口：`PROJECT_SPEC.md`
- 需求文档：`docs/REQUIREMENTS.md`
- 子代理协作：`docs/AGENT_COLLABORATION.md`
- 上下文交接：`docs/CONTEXT_HANDOFF.md`
- 原始页面：`controller-review(1).html`

## 恢复工作时先读

如果发生上下文压缩、会话恢复或更换代理，先读：

1. `PROJECT_SPEC.md`
2. `docs/CONTEXT_HANDOFF.md`
3. `docs/REQUIREMENTS.md`
4. `docs/AGENT_COLLABORATION.md`

确认当前仍处于哪个阶段后再行动。
