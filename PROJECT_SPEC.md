# 项目规范

## 项目定位

本项目当前保留一个静态 HTML 展示页作为历史基线，用于呈现控制器横向测试结果。1.1.1 已迁移为原生抖音小程序项目。

页面目标风格为测评报告：强调数据可信、信息层级清晰、移动端阅读舒适，不做营销落地页，也不做重后台数据看板。

## 版本

- 当前版本：1.1.2
- 当前 Git 标签：`v1.1.2`
- GitHub 仓库：`https://github.com/wentyang911331-alt/douyin-miniapp-controller-review`

每个正式版本都必须完成代码备份：提交本地 Git、创建版本标签、推送代码和标签到 GitHub。需要回滚时，优先从 GitHub 对应标签恢复。

## 1.1.1 历史基线口径

- 页面标题：2026 年春季控制线横屏对比手册
- 水印文字：小拓子横评
- 说明展示方式：每个测试模块标题旁放置信息图标，点击后显示小浮窗说明
- 小程序迁移口径：1.1.1 迁移为原生抖音小程序，不使用 `web-view`
- UI 方向：测评报告
- 原始页面文件：`controller-review(1).html`

## 1.1.2 已确认调整口径

- 页面标题：2026年春季260级控制器对比手册
- 水印文字：小拓子
- 说明展示方式：模块标题旁信息图标，点击后以居中弹窗展示说明
- 视觉方向：测评报告风格，整体高亮改为图标蓝，并同步调整蓝色同色系背景、暗色字体和低纯度低明度蓝灰辅色；主题色底的选中标签统一白字
- 实施范围：只开发当前原生抖音小程序入口，`controller-review(1).html` 继续作为历史基线保留

## 当前边界

当前已经完成 1.1.1 原生抖音小程序迁移。后续新增需求应先记录到需求文档，再按版本推进开发、验证和备份。

当前不需要后端。现有页面数据全部内置在静态文件中，没有接口、数据库、账号状态、动态配置或后台管理需求。如后续出现这些需求，再引入后端子代理。

1.1.2 开发必须先经过 UI 验收门：UI 子代理使用 Figma 插件产出方案，用户验收通过后，才允许修改当前运行入口代码。当前 UI 已验收通过，前端实现已完成。

1.1.2 UI 验收稿已生成：`https://www.figma.com/design/hT8h1tjecsM2zBVmBCXUQD`。当前状态为已验收通过。

## 1.1.2 实现结果

1.1.2 是原生抖音小程序迁移后的视觉、交互和说明文案修补版本，当前已完成实现。

重点包括：标题改为 `2026年春季260级控制器对比手册`，模块导航从数字改为简称，修复明亮模式图标和导航栏颜色，调整二级标题图标、表格/排行榜交互、长文案溢出、升序/降序布局，统一图标蓝高亮与低饱和灰蓝配色体系，恢复 `小拓子` 水印，并将说明浮窗改为居中弹窗。

1.1.2 未接入后端、未引入第三方依赖、未修改测试数据。

## 1.1.1 迁移结果

1.1.1 已从静态 HTML 迁移为原生抖音小程序项目。迁移后，`controller-review(1).html` 作为历史基线保留，不再作为运行入口。

小程序代码使用 `app.js`、`app.json`、`app.ttss`、`project.config.json` 和 `pages/controller-review/` 下的 `.ttml`、`.ttss`、`.js`、`.json` 文件组织。

1.1.1 不使用 `web-view` 包裹 H5，不使用浏览器 DOM 写法，不接入后端，不引入第三方依赖。

## 小程序准备原则

后续小程序开发时，应遵循：

- 数据和展示逻辑分离，避免堆叠难维护的字符串拼接
- 状态变更集中管理，使用 `Page`、`data`、`setData`、`bindtap`
- 样式保持移动端优先，使用 `.ttss`
- 不引入浏览器重型依赖或第三方渲染库
- 不使用 `window`、`document`、`innerHTML`、浏览器 `localStorage`
- 保留 1.1.0 静态页面作为对照和回滚参考

## UI 原则

- 页面应像一份可阅读的测评报告，而不是广告页
- 标题、分区标题、表格、排行榜、提示说明要形成明确层级
- 水印要弱化处理，不影响数据阅读
- 模块说明默认隐藏，通过信息图标唤起居中弹窗
- 说明弹窗内容应简短、具体、说明测试口径，不堆长段落
- 移动端优先，重点保证 400px 左右宽度下的阅读体验
- 交互按钮保持紧凑，但可点击区域不能过小
- 表格横向滚动、固定首列、高亮产品、排名显示等现有核心能力应保留

## 子代理协作口径

当前阶段采用两个子代理：

- UI 子代理：负责视觉风格、信息层级、水印、弹窗、报告感细节
- 前端子代理：负责代码结构、交互实现、小程序准备约束、实现风险

主控负责调度、合并冲突、确认边界、最终落地。任何修改当前运行入口代码的动作，都必须在需求和方案确认之后进行。

## 文档入口

- 需求文档：`docs/REQUIREMENTS.md`
- 子代理协作文档：`docs/AGENT_COLLABORATION.md`
- 上下文交接文档：`docs/CONTEXT_HANDOFF.md`
