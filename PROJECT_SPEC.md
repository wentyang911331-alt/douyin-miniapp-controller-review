# 项目规范

## 项目定位

本项目当前是一个静态 HTML 展示页，用于呈现控制线横向测试结果。当前阶段先保留 HTML 展示形态，不迁移为完整抖音小程序项目。

页面目标风格为测评报告：强调数据可信、信息层级清晰、移动端阅读舒适，不做营销落地页，也不做重后台数据看板。

## 版本

- 当前版本：1.1.0
- 当前 Git 标签：`v1.1.0`
- GitHub 仓库：`https://github.com/wentyang911331-alt/douyin-miniapp-controller-review`

每个正式版本都必须完成代码备份：提交本地 Git、创建版本标签、推送代码和标签到 GitHub。需要回滚时，优先从 GitHub 对应标签恢复。

## 已确认内容

- 页面标题：2026 年春季控制线横屏对比手册
- 水印文字：小拓子横评
- 说明展示方式：每个测试模块标题旁放置信息图标，点击后显示小浮窗说明
- 小程序准备口径：保留 HTML，同时尽量按抖音小程序页面的结构、样式和交互习惯约束后续改动
- UI 方向：测评报告
- 原始页面文件：`controller-review(1).html`

## 当前边界

当前已经完成 1.1.0 页面实现。后续新增需求应先记录到需求文档，再按版本推进开发、验证和备份。

当前不需要后端。现有页面数据全部内置在静态文件中，没有接口、数据库、账号状态、动态配置或后台管理需求。如后续出现这些需求，再引入后端子代理。

## 小程序准备原则

后续修改 HTML 时，应尽量让代码接近未来可迁移到抖音小程序的形态：

- 数据和展示逻辑分离，避免继续堆叠难维护的字符串拼接
- 状态变更集中管理，便于未来迁移为 `Page`、`data`、`setData`、`bindtap`
- 样式保持移动端优先，类名清晰，便于未来拆分为 `.ttss`
- 不引入浏览器重型依赖或第三方渲染库
- 尽量减少对 `window`、`document`、`innerHTML`、`localStorage` 的新增依赖
- 保留当前静态页面可直接打开和演示的能力

## UI 原则

- 页面应像一份可阅读的测评报告，而不是广告页
- 标题、分区标题、表格、排行榜、提示说明要形成明确层级
- 水印要弱化处理，不影响数据阅读
- 模块说明默认隐藏，通过信息图标唤起小浮窗
- 小浮窗内容应简短、具体、说明测试口径，不堆长段落
- 移动端优先，重点保证 400px 左右宽度下的阅读体验
- 交互按钮保持紧凑，但可点击区域不能过小
- 表格横向滚动、固定首列、高亮产品、排名显示等现有核心能力应保留

## 子代理协作口径

当前阶段采用两个子代理：

- UI 子代理：负责视觉风格、信息层级、水印、小浮窗、报告感细节
- 前端子代理：负责代码结构、交互实现、小程序准备约束、实现风险

主控负责调度、合并冲突、确认边界、最终落地。任何修改原始 HTML 的动作，都必须在需求和方案确认之后进行。

## 文档入口

- 需求文档：`docs/REQUIREMENTS.md`
- 子代理协作文档：`docs/AGENT_COLLABORATION.md`
- 上下文交接文档：`docs/CONTEXT_HANDOFF.md`
